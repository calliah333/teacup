services:
  teacup:
    build: .
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - ./uploads:/app/uploads
      - ./.env:/app/.env:ro
    environment:
      - UPLOAD_DIR=/app/uploads
    restart: unless-stopped
